{
  "name": "klimo",
  "private": true,
  "description": "Klimo — dimensionnement de climatisation pour installateurs (Toshiba actif, Panasonic conservé en réserve) — PWA statique, aucune étape de build pour le déploiement.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
